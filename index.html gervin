<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jollibee Fan Site | Spreading Joy</title>
    <style>
        /* Jollibee Brand Colors */
        :root {
            --jolly-red: #E31837;
            --jolly-yellow: #FFC72C;
            --white: #FFFFFF;
            --dark: #231F20;
        }

        body {
            font-family: 'Arial Rounded MT Bold', 'Helvetica', sans-serif;
            margin: 0;
            background-color: #f9f9f9;
        }

        /* Navigation Bar */
        nav {
            background-color: var(--jolly-red);
            padding: 15px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        nav .logo {
            color: var(--white);
            font-size: 24px;
            font-weight: bold;
            text-decoration: none;
        }

        nav ul {
            list-style: none;
            display: flex;
            margin: 0;
        }

        nav ul li {
            margin-left: 20px;
        }

        nav ul li a {
            color: var(--white);
            text-decoration: none;
            font-weight: bold;
        }

        /* Hero Section */
        .hero {
            background-color: var(--jolly-yellow);
            text-align: center;
            padding: 60px 20px;
        }

        .hero h1 {
            color: var(--jolly-red);
            font-size: 3rem;
            margin-bottom: 10px;
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--dark);
        }

        /* Menu Grid */
        .menu-container {
            max-width: 1000px;
            margin: 40px auto;
            padding: 0 20px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .menu-card {
            background: var(--white);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s;
            text-align: center;
        }

        .menu-card:hover {
            transform: translateY(-10px);
        }

        .menu-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .menu-card h3 {
            color: var(--jolly-red);
            margin: 15px 0 5px 0;
        }

        .menu-card p {
            color: #666;
            font-size: 0.9rem;
            padding: 0 10px 15px 10px;
        }

        .order-btn {
            background-color: var(--jolly-red);
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 20px;
            cursor: pointer;
            margin-bottom: 20px;
            font-weight: bold;
        }

        footer {
            background: var(--dark);
            color: white;
            text-align: center;
            padding: 20px;
            margin-top: 50px;
        }
    </style>
</head>
<body>

    <nav>
        <a href="#" class="logo">JOLLIBEE</a>
        <ul>
            <li><a href="#">Menu</a></li>
            <li><a href="#">Promos</a></li>
            <li><a href="#">Delivery</a></li>
        </ul>
    </nav>

    <header class="hero">
        <h1>Bida ang Saya!</h1>
        <p>Bringing the joy of eating to everyone.</p>
    </header>

    <section class="menu-container">
        <div class="menu-card">
            <img src="https://via.placeholder.com/300x200?text=Chickenjoy" alt="Chickenjoy">
            <h3>Chickenjoy</h3>
            <p>Our signature crispy and juicy fried chicken.</p>
            <button class="order-btn">Order Now</button>
        </div>

        <div class="menu-card">
            <img src="https://via.placeholder.com/300x200?text=Jolly+Spaghetti" alt="Jolly Spaghetti">
            <h3>Jolly Spaghetti</h3>
            <p>Sweet-style spaghetti with ham and hotdog slices.</p>
            <button class="order-btn">Order Now</button>
        </div>

        <div class="menu-card">
            <img src="https://via.placeholder.com/300x200?text=Yumburger" alt="Yumburger">
            <h3>Yumburger</h3>
            <p>The classic burger with our special dressing.</p>
            <button class="order-btn">Order Now</button>
        </div>
    </section>

    <footer>
        <p>&copy; 2026 Fan Project - Inspired by Jollibee Foods Corporation</p>
    </footer>

</body>
</html>
